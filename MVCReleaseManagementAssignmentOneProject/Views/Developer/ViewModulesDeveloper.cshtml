@model IEnumerable<MVCReleaseManagementAssignmentOneProject.Models.ModuleDetails>



<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>ViewModulesDeveloper</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ModuleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProjectId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DeveloperId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ModuleStatus)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ModuleId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ProjectId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DeveloperId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ModuleStatus)
                </td>

            </tr>
        }

    </table>
    <br />
    <div id="navlink">
        @Html.ActionLink("Go Back to Menu", "DeveloperNavigate")
    </div>
</body>
</html>
